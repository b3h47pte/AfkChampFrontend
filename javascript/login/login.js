var rocketelo = angular.module('rocketelo', ['ui.utils']);

rocketelo.controller('LoginController', function($scope) {
  // LOGIN FUNCTIONALITY
  $scope.login = function(user) {
  
  };
  
  
  // REGISTER FUNCTIONALITY
  $scope.register = function(user) {
  
  }

});